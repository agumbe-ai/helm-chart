apiVersion: v1
kind: Service
metadata:
  name: {{ include "xnamespace-operator.fullname" . }}
  labels:
    {{- include "xnamespace-operator.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "xnamespace-operator.selectorLabels" . | nindent 4 }}
